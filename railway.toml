[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health/ready"
healthcheckTimeout = 300
preDeployCommand = "sh -c 'alembic upgrade head && python scripts/bootstrap_admin.py'"
